"""The harness-facing side of the board: the tools and event handlers a session gets, and their ``activate``.

One module per former extension shell, basename kept, so each keeps the descriptor name it
had; ``ally/`` beside this is the same for allies (someone else's CLI run as a Sister).
"""
